预期:
── 新版 Bybit Bot(用于部署在vercel版本的)
    │
    ├── /id
    ├── /buy
    ├── /sell
    └── /status
        │
        ↓
        Vercel
        │
        ↓
    Bybit Testnet 🧪

用户 → /id
🤖 → 你的 ID：123456789

用户 → /status
🤖 → 🟢 Bot 正常
     🧪 Bybit Testnet
     💰 USDTUSD

用户 → /buy
🤖 → 🧪 TESTNET
     买入 USDTUSD
     数量：1 USDT
     ✅ 下单成功

用户 → /sell
🤖 → 🧪 TESTNET
     卖出 USDTUSD
     数量：1 USDT
     ✅ 下单成功

Telegram 发来命令
       ↓
先检查 Telegram ID
       ↓
是不是主人？
   ↙       ↘
  是        不是
  ↓          ↓
执行       ❌ 拒绝
  ↓
Bybit Testnet

# Telegram × Bybit Vercel 小机器人项目企划书

## 一、项目想干什么？

一句话：

**把 Telegram 变成 Bybit 测试网的“遥控器”。**

现在已有 `tg_id_Vercel` 项目，主要功能是通过 Telegram Bot 获取用户自己的 Telegram ID，并部署在 Vercel。

下一步计划不破坏现有 `main`，而是在同一个 GitHub 仓库中新建：

**`bybit` branch**

这个分支专门开发新版机器人，把原来的“查 ID”升级成：

**查 ID + 查看状态 + 遥控 Bybit 测试网下单。**

整个项目仍然部署在 Vercel，不需要自己准备一台 24 小时运行的服务器，也不需要 Docker 常驻运行。

---

## 二、它怎么工作？

可以把整个系统想象成一家“无人值守的小银行”。

Telegram 是**遥控器**，Vercel 是**值班经理**，Bybit Testnet 是**模拟交易柜台**。

流程非常简单：

**我 → Telegram → Vercel → Bybit Testnet**

例如我在 Telegram 输入：

`/buy`

Telegram 就像快递员一样，把这张“买入申请单”送到 Vercel 的 Webhook。

Vercel 被叫醒后，先检查：

**“这个人是不是主人？”**

如果 Telegram ID 不在白名单里：

**❌ 拒绝操作。**

如果身份正确：

**✅ 调用 Bybit API → 测试网下单 → 把结果返回 Telegram。**

处理结束后 Vercel 继续“睡觉”，等待下一次 Telegram 消息。

这正好符合 Vercel Serverless 的工作方式。

---

## 三、第一版机器人有什么功能？

先做小、做稳，不追求一步登天。

### `/id`

查看自己的 Telegram ID。

相当于机器人回答：

**“你是谁？”**

### `/status`

检查机器人目前是否正常，例如：

**🟢 Bot：正常
🧪 Bybit：TESTNET
📡 API：已连接**

### `/buy`

向 Bybit 测试网发送固定的小额买入指令。

例如：

**🧪 TESTNET
📈 BUY USDTUSD
💰 数量：1 USDT
✅ 下单成功**

### `/sell`

与 `/buy` 相反，发送测试网卖出指令。

第一阶段**只做 Testnet，不接真钱实盘**。

---

## 四、安全怎么保证？

这是项目最重要的一道门。

Vercel 环境变量保存：

**Telegram BOT_TOKEN
Bybit API KEY
Bybit API SECRET
允许操作的 Telegram ID**

这些秘密**绝不写死在 GitHub 代码里**。

机器人收到 `/buy`、`/sell` 后，也不能直接执行，而是：

**收到命令 → 检查 Telegram ID → 白名单通过 → 才允许访问 Bybit**

可以理解成：

**Telegram 是大门，Telegram ID 是门禁卡，Bybit API Key 是保险柜钥匙。**

陌生人就算找到机器人，也拿不到保险柜钥匙。

---

## 五、GitHub 怎么安排？

仓库继续使用：

**`tg_id_Vercel`**

结构简单保持为：

**🌿 main**
保留现在已经能工作的 Telegram ID 查询机器人。

**🌿 bybit**
开发 Vercel 版 Bybit Testnet 机器人。

这样最大的好处就是：

**老版本不动，新版本大胆实验。**

如果 `bybit` 写坏了，直接回 `main`，原来的项目仍然存在。

等新版完全稳定后，再决定是否合并。

---

## 六、最终目标

这个项目不是要一开始做成复杂的量化交易平台。

第一阶段只解决一件事：

**让我在 Telegram 里安全、简单、直观地控制自己的 Bybit 测试账户。**

最终体验应该像拿着一个遥控器：

**📱 Telegram**

`/id` → 我是谁？
`/status` → 机器活着吗？
`/buy` → 测试买入
`/sell` → 测试卖出

背后的 Vercel、Webhook、API 全部藏起来。

**前面四个按钮，后面自动干活。**

项目路线也很明确：

**先能跑 → 再安全 → 再好用 → 最后才考虑更多交易功能。**

第一阶段的成功标准只有一句话：

> **打开 Telegram，发一个 `/buy`，几秒后收到 Bybit Testnet 下单成功的回复。**
